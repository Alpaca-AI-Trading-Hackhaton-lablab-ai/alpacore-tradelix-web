VITE_APP_TITLE=TradeLix AI
VITE_API_URL=/api
VITE_DEFAULT_SYMBOL=SPY

# Host port for the nginx container (always :80 inside). Compose default is 3200.
# Local: leave unset or 3200. EC2 box (domain with no port): WEB_PORT=80
# WEB_PORT=3200
